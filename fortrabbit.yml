---
# For future back-compatability
version: 1

# Use full sync strategy, so git-deleted files are removed on the server
strategy: fullsync

composer:
   mode: trigger
   method: install

post-deploy:
   script: setup.php
   args: []
